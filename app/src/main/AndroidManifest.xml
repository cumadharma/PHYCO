<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gmmre.phyco">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".DaftarActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".KonsultasiPasienActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ConfirmMasalahActivity" />
        <activity android:name=".SolusiActivity" />
        <activity android:name=".EditSolusiActivity" />
        <activity android:name=".RiwayatKonsultasiActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".TambahPasienActivity" />
        <activity android:name=".PengaturanActivity" />
        <activity android:name=".TentangActivity"></activity>
    </application>

</manifest>